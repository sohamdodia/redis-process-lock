{
  "name": "redis-process-locks",
  "version": "1.0.0",
  "description": "A NPM module that will lock the process.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ioredis": "^4.17.3"
  }
}
